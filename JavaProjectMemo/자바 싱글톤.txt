자바 싱글톤

회사라는 클래스를 만들고 데이터를 만들고 다른 클래스에서 getter,setter를 통해 가져온다.
=> 그러면 무결성이 유지되나 => 안된다.

싱글톤 패턴의 목적은 한 번 객체를 생성하고 생성된 인스턴스를 각 클래스가 가져가도록 한다.
=> 여러번 객체생성를 해서 주지 않는다.
=> 동기화에 유리하다.

=> 기존에 생성된 객체를 다른 클래스들이 가져다 쓰라는 것이다.

getInstance() 메서드를 통해서만 가져올 수 있다.