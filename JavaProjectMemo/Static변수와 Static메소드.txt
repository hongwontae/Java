Static변수와 Static메소드

일반적으로 클래스는 Static 영역에 new 연산자를 통해 만든 객체는 heap영역에 만들어진다. 
Static 영역은 가비지 컬렉터x heap은 가비지 컬렉터 o => 
Static 영역에 할당된 메모리 모든 객체가 공유하는 메모리로 작용 => 많으면 성능 저하

Static 변수, 메소드
=> new 연산자 없이 그냥 사용하면 된다. 이미 메모리 상에 올라와 있다.
